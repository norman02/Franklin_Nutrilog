package nutrilog;
import java.sql.*;
public class command {

    // Information page controller
    /*
     * SELECT
     * FROM
     * event
     * LEFT JOIN
     * Person ON Person.id = Event.Personid;
     */
    
    
    /*
     *Search Function Controller
     * "SELECT * FROM person
     *WHERE Person.id = ?;"

    " SELECT * FROM person
    WHERE LastName = ?;"*
     */
    
    /*
     * Create a Controller that adds a new entry to the Person table. Use the following command as a baseline:

"INSERT INTO Person (id, FirstName, LastName, Gender)"git ch
     */
}
